---
schemaVersion: 1
contentModel: systemCriterion
contentModelVersion: 1

criterion:
  code: D-05
  slug: d-05
  title: 비밀번호 재사용에 대한 제약 설정
  severity:
    level: medium
    sourceLabel: 중

classification:
  domainIdentifier: dbms
  categoryIdentifier: dbms-account-management

targetScope: nonExhaustive
targetIdentifiers:
  - solaris
  - linux
  - aix
  - hp-ux
sourceTargetText: "Oracle DB, Altibase, Tibero 등"

provenance:
  sourceDocumentIdentifier: kisa-cce-criteria-2026
  sourcePageRanges:
    - physicalPageStart: 613
      physicalPageEnd: 615
      printedPageStart: "613"
      printedPageEnd: "615"

sourceAnnotations:
  - annotationIdentifier: d-05-source-001
    annotationType: sourceInconsistency
    targetType: astNode
    targetReference: "d-05:remediation.supplementaryGuidance.command:1"
    sourceLocation:
      physicalPage: 613
      printedPage: "613"
      pageRegionIdentifier: p613-d-05
    sourceText: "PASSWORD_REUSE_MAX' A"
    explanation: "Oracle DB 점검 SQL의 단어가 원문 행 경계에서 분리되어 있다. 원문 표기를 그대로 보존했다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical pages 613-614의 Oracle DB Step 1 명령문을 확인했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
  - annotationIdentifier: d-05-source-002
    annotationType: sourceInconsistency
    targetType: astNode
    targetReference: "d-05:remediation.supplementaryGuidance.command:4"
    sourceLocation:
      physicalPage: 614
      printedPage: "614"
      pageRegionIdentifier: p614-d-05
    sourceText: "PASSWORD_LOCK_TIM\nE 7"
    explanation: "Altibase 예시의 PASSWORD_LOCK_TIME이 원문 행 경계에서 분리되어 있다. 원문 표기를 그대로 보존했다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical page 614의 Altibase Step 2 예시를 확인했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
  - annotationIdentifier: d-05-source-003
    annotationType: sourceInconsistency
    targetType: astNode
    targetReference: "d-05:remediation.supplementaryGuidance.command:6"
    sourceLocation:
      physicalPage: 615
      printedPage: "615"
      pageRegionIdentifier: p615-d-05
    sourceText: "SELECT * FROM dba_users;"
    explanation: "Tibero Step 2는 PROFILE 설정 내용 확인 절차이지만 Step 1과 같은 조회문을 제시하며, 화면 예시는 PROFILE 자원 설정을 표시한다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical page 615의 Tibero Step 1과 Step 2 명령문 및 화면 예시를 대조했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
---

## 개요

### 점검 내용

비밀번호 변경 시 이전 비밀번호를 재사용할 수 없도록 비밀번호 제약 설정이 되어있는지 점검

### 점검 목적

비밀번호 재사용 제약 설정 적용 여부를 점검하여 비밀번호 변경 시 이전 비밀번호 재사용을 제약하여 형식적인 비밀번호 변경을 원천적으로 차단하기 위함

### 보안 위협

비밀번호 재사용 제약 설정이 적용되어 있지 않을 경우 비밀번호 변경 전 사용했던 비밀번호를 재사용함으로써 비인가자의 계정 비밀번호 추측 공격에 대한 시간을 더 많이 허용하여 비밀번호 유출 위험이 증가함

### 참고

> **참고**
>
> **비밀번호 제약 설정:** 비밀번호 변경 시 이전에 사용했던 비밀번호를 재사용할 수 없게 하는 설정으로써 이전 암호 재사용 가능 기간(`PASSWORD_REUSE_TIME`), 이전 암호 재사용 가능 횟수(`PASSWORD_REUSE_MAX`) 등이 있음

## 점검 대상 및 판단 기준

### 대상

Oracle DB, Altibase, Tibero 등

### 판단 기준

- **양호:** 비밀번호 재사용 제한 설정을 적용한 경우
- **취약:** 비밀번호 재사용 제한 설정을 적용하지 않은 경우

### 조치 방법

`PASSWORD_REUSE_TIME`, `PASSWORD_REUSE_MAX` 파라미터 설정

### 조치 시 영향

일반적인 경우 영향 없음

## 점검 및 조치 사례

### 추가 지침

**Oracle DB**

1. SQL*Plus 설정 확인

   ```sql command
   -- Check for both reuse max and reuse time not set
   SELECT profile FROM DBA_PROFILES WHERE (resource_name = 'PASSWORD_REUSE_MAX' A
   ND limit IN ('UNLIMITED', 'NULL')) OR (profile IN (SELECT profile FROM DBA_PROFILES WHE
   RE resource_name = 'PASSWORD_REUSE_TIME') AND limit IN ('UNLIMITED', 'NULL'));

   -- Check for reuse max with value that is less than allowed minimum
   SELECT profile FROM DBA_PROFILES WHERE resource_name = 'PASSWORD_REUSE_MAX' AN
   D limit NOT IN ('UNLIMITED', 'NULL') AND REGEXP_LIKE(limit, '^[0-9]+$') -- Only consider nume
   ric values AND TO_NUMBER(limit) < 10;

   -- Check for reuse time that is less than allowed minimum
   SELECT profile FROM DBA_PROFILES WHERE resource_name = 'PASSWORD_REUSE_TIME' AN
   D limit NOT IN ('UNLIMITED', 'NULL') AND REGEXP_LIKE(limit, '^[0-9]+$') -- Only consider nume
   ric values AND TO_NUMBER(limit) < 365;
   ```

2. `PASSWORD_REUSE_TIME` 및 `PROFILE` 파라미터 수정

   ```sql command
   SQL> ALTER PROFILE default LIMIT password_reuse_time 365 password_reuse_max 10;
   SQL> ALTER PROFILE [프로파일명] LIMIT password_reuse_time DEFAULT password_reuse_max
   default;
   ```

**Altibase**

1. 다음 명령어를 통해 비밀번호 정책 설정 여부 확인

   ```sql command
   SELECT * FROM system_.sys_users_;
   ```

2. 아래 Property에 대해 비밀번호 정책 설정

   ```text configuration
   CASE_SENSITIVE_PASSWORD
   FAILED_LOGIN_ATTEMPTS
   PASSWORD_LOCK_TIME
   PASSWORD_LIFE_TIME
   PASSWORD_GRACE_TIME
   PASSWORD_REUSE_TIME
   PASSWORD_REUSE_MAX
   PASSWORD_VERIFY_FUNCTION
   ```

   정책 적용 시 다음 명령어를 사용

   ```sql command
   ALTER USER [계정명] LIMIT (Property 숫자);
   예시) ALTER USER TESTUSER LIMIT (FAILED_LOGIN_ATTEMPTS 7, PASSWORD_LOCK_TIM
   E 7);
   ```

**Tibero**

1. 사용자별 비밀번호 `PROFILE` 적용 여부 확인

   ```sql command
   SELECT * FROM dba_users;
   ```

   ```text output
   [ 사용자별 비밀번호 PROFILE 적용 여부 확인 ]
   ```

2. 설정되어 있을 경우 `PROFILE` 설정 내용 확인

   ```sql command
   SELECT * FROM dba_users;
   ```

   ```text output
   [ PASSWORD_REUSE_TIME, PASSWORD_REUSE_MAX 파라미터값 확인 ]
   ```

3. 설정되어 있지 않을 경우 `PROFILE` 생성 또는 수정 시(`ALTER PROFILE`) 비밀번호 정책 설정 적용 시 다음 명령어를 사용

   ```sql command
   CREATE PROFILE prof LIMIT
   예시) CREATE PROFILE prof LIMIT
   failed_login_attempts 3
   password_lock_time 1/1440
   password_life_time 90
   password_reuse_time unlimited
   password_reuse_max 10
   password_grace_time 10
   password_verify_function verify_function;
   ```
