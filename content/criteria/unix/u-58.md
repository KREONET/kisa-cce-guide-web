---
schemaVersion: 1
contentModel: systemCriterion
contentModelVersion: 1

criterion:
  code: U-58
  slug: u-58
  title: 불필요한 SNMP 서비스 구동 점검
  severity:
    level: medium
    sourceLabel: 중

classification:
  domainIdentifier: unix
  categoryIdentifier: unix-service-management

targetScope: nonExhaustive
targetIdentifiers:
  - solaris
  - linux
  - aix
  - hp-ux
sourceTargetText: "SOLARIS, LINUX, AIX, HP-UX 등"

provenance:
  sourceDocumentIdentifier: kisa-cce-criteria-2026
  sourcePageRanges:
    - physicalPageStart: 141
      physicalPageEnd: 142
      printedPageStart: "141"
      printedPageEnd: "142"

sourceAnnotations: []
---

## 개요

### 점검 내용

SNMP 서비스 활성화 여부 점검

### 점검 목적

불필요한 SNMP 서비스를 비활성화하여 필요 이상의 정보가 노출되는 것을 방지하기 위함

### 보안 위협

SNMP 서비스가 활성화되어 있을 경우, 비인가자가 시스템의 중요 정보를 유출하거나 불법적으로 수정할 위험이 존재함

### 참고

> **참고**
>
> **SNMP(Simple Network Management Protocol):** TCP/IP 기반 네트워크상의 각 호스트에서 정기적으로 여러 정보를 자동으로 수집하여 네트워크 관리를 하기 위한 프로토콜을 의미함

> **참고**
>
> 기반시설 시스템에서 SNMP 서비스의 이용은 원칙적으로 금지하나, 불가피하게 사용 시 기본 Comunity String 변경, 네트워크 모니터링 등의 보안 조치를 반드시 적용해야 함

## 점검 대상 및 판단 기준

### 대상

SOLARIS, LINUX, AIX, HP-UX 등

### 판단 기준

- **양호:** SNMP 서비스를 사용하지 않는 경우
- **취약:** SNMP 서비스를 사용하는 경우

### 조치 방법

SNMP 서비스를 사용하지 않는 경우 서비스 중지 및 비활성화 설정

### 조치 시 영향

일반적인 경우 영향 없음

## 점검 및 조치 사례

### SOLARIS

**5.9 이하 버전**

1. SNMP 서비스 활성화 여부 및 경로 확인

   ```shell command
   ps -ef | grep snmp
   ```

2. 서비스 중지 및 이름 변경

   ```shell command
   /etc/init.d/init.snmpdx stop
   mv /etc/rc3.d/S76snmpdx /etc/rc3.d/_S76snmpdx
   ```

> **참고**
>
> `rc*/_S**snmpdx`의 `*`수치는 각각 다름

**5.10 이상 버전**

1. SNMP 서비스 활성화 여부 확인

   ```shell command
   svcs -a | grep snmp
   ```

2. 불필요한 SNMP 서비스가 활성화 중인 경우 데몬 중지

   ```shell command
   svcadm disable svc:/application/management/snmpd:default
   ```

### LINUX

1. SNMP 서비스 활성화 여부 확인

   ```shell command
   systemctl list-units --type=service | grep snmpd
   ```

2. 불필요한 SNMP 서비스가 활성화(`loaded active running`)인 경우 서비스 중지 및 비활성화

   ```shell command
   systemctl stop snmpd
   systemctl disable snmpd
   ```

### AIX

1. SNMP 서비스 활성화 여부 확인

   ```shell command
   lssrc -a | grep snmp
   ```

2. 불필요한 SNMP 서비스가 활성화(`active`) 중인 경우 서비스 중지

   ```shell command
   stopsrc –s snmpd
   ```

3. `/etc/rc.tcpip` 파일 내에 SNMP설정값 주석 처리

   ```shell configuration
   # start /usr/sbin/snmpd $src_running
   ```

4. 설정 적용

   ```shell command
   /etc/rc.tcpip
   ```

### HP-UX

1. SNMP 서비스 활성화 여부 확인

   ```shell command
   ps -ef | grep snmp
   ```

2. 불필요한 SNMP 서비스가 활성화(`active`) 중인 경우 서비스 중지

   ```shell command
   /sbin/init.d/snmpd stop
   ```
