---
schemaVersion: 1
contentModel: systemCriterion
contentModelVersion: 1

criterion:
  code: U-63
  slug: u-63
  title: sudo 명령어 접근 관리
  severity:
    level: medium
    sourceLabel: 중

classification:
  domainIdentifier: unix
  categoryIdentifier: unix-service-management

targetScope: nonExhaustive
targetIdentifiers:
  - solaris
  - linux
  - aix
  - hp-ux
sourceTargetText: "SOLARIS, LINUX, AIX, HP-UX 등"

provenance:
  sourceDocumentIdentifier: kisa-cce-criteria-2026
  sourcePageRanges:
    - physicalPageStart: 159
      physicalPageEnd: 159
      printedPageStart: "159"
      printedPageEnd: "159"

sourceAnnotations:
  - annotationIdentifier: u-63-source-001
    annotationType: sourceInconsistency
    targetType: astNode
    targetReference: "u-63:remediation.solaris.command:2"
    sourceLocation:
      physicalPage: 159
      printedPage: "159"
      pageRegionIdentifier: p159-u-63
    sourceText: "/etc/sudoer"
    explanation: "같은 단계의 설명과 앞선 명령은 /etc/sudoers를 가리키지만, 이 명령은 /etc/sudoer를 가리킨다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical page 159의 Step 2 설명과 두 명령을 대조했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
  - annotationIdentifier: u-63-source-002
    annotationType: sourceOmission
    targetType: astNode
    targetReference: "u-63:remediation.solaris.step:3"
    sourceLocation:
      physicalPage: 159
      printedPage: "159"
      pageRegionIdentifier: p159-u-63
    sourceText: "s"
    explanation: "절차의 의미를 판별할 수 없는 불완전한 단계가 원문에 표시되어 있다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical page 159의 점검 및 조치 사례 하단을 확인했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
---

## 개요

### 점검 내용

`/etc/sudoers` 파일 권한 적절성 여부 점검

### 점검 목적

비인가자가 관리자 권한을 남용하여 시스템 손상, 악성 코드 실행, 민감한 데이터 유출 등의 보안 위협을 방지하기 위함

### 보안 위협

`sudo` 명령어 접근을 제한하지 않을 경우, 비인가자가 관리자 권한으로 허가되지 않은 명령어를 사용하여 루트 권한 오용, 악성 코드 실행, 데이터 유출 등의 시도를 할 위험이 존재함

### 참고

> **참고**
>
> **sudo(SuperUser DO):** root 권한으로 명령어를 실행함

> **참고**
>
> **`/etc/sudoers`:** `sudo` 명령을 사용하여 다른 명령을 실행할 수 있는 사용자를 지정하는 파일

## 점검 대상 및 판단 기준

### 대상

SOLARIS, LINUX, AIX, HP-UX 등

### 판단 기준

- **양호:** `/etc/sudoers` 파일 소유자가 root이고, 파일 권한이 `640`인 경우
- **취약:** `/etc/sudoers` 파일 소유자가 root가 아니거나, 파일 권한이 `640`을 초과하는 경우

### 조치 방법

`/etc/sudoers` 파일 소유자 및 권한 변경 설정

### 조치 시 영향

일반적인 경우 영향 없음

## 점검 및 조치 사례

### SOLARIS

1. `/etc/sudoers` 파일 소유자 및 권한 확인

   ```shell command
   # ls -l /etc/sudoers
   ```

2. `/etc/sudoers` 파일 소유자를 root, 권한 `640`으로 변경

   ```shell command
   # chown root /etc/sudoers
   # chmod 640 /etc/sudoer
   ```

3. s

### LINUX

1. `/etc/sudoers` 파일 소유자 및 권한 확인

   ```shell command
   # ls -l /etc/sudoers
   ```

2. `/etc/sudoers` 파일 소유자를 root, 권한 `640`으로 변경

   ```shell command
   # chown root /etc/sudoers
   # chmod 640 /etc/sudoer
   ```

3. s

### AIX

1. `/etc/sudoers` 파일 소유자 및 권한 확인

   ```shell command
   # ls -l /etc/sudoers
   ```

2. `/etc/sudoers` 파일 소유자를 root, 권한 `640`으로 변경

   ```shell command
   # chown root /etc/sudoers
   # chmod 640 /etc/sudoer
   ```

3. s

### HP-UX

1. `/etc/sudoers` 파일 소유자 및 권한 확인

   ```shell command
   # ls -l /etc/sudoers
   ```

2. `/etc/sudoers` 파일 소유자를 root, 권한 `640`으로 변경

   ```shell command
   # chown root /etc/sudoers
   # chmod 640 /etc/sudoer
   ```

3. s
