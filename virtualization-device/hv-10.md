---
schemaVersion: 1
contentModel: systemCriterion
contentModelVersion: 1

criterion:
  code: HV-10
  slug: hv-10
  title: SNMP Community String 복잡성 적용
  severity:
    level: medium
    sourceLabel: 중

classification:
  domainIdentifier: virtualization-device
  categoryIdentifier: virtualization-device-system-service-management

targetScope: nonExhaustive
targetIdentifiers:
  - linux
sourceTargetText: "VMware ESXi, vCenter, XenServer, KVM, Nutanix"

provenance:
  sourceDocumentIdentifier: kisa-cce-criteria-2026
  sourcePageRanges:
    - physicalPageStart: 819
      physicalPageEnd: 823
      printedPageStart: "819"
      printedPageEnd: "823"

sourceAnnotations:
  - annotationIdentifier: hv-10-source-001
    annotationType: sourceTypographicalError
    targetType: astNode
    targetReference: "hv-10:remediation.linux.command:4"
    sourceLocation:
      physicalPage: 820
      printedPage: "820"
      pageRegionIdentifier: p820-hv-10
    sourceText: "grep –E \"SNMPCommunityString|SNMPAccessControl“"
    explanation: "명령의 옵션 앞 문자가 하이픈(-)이 아닌 en dash(–)로 표기되어 있고, 인용 부호가 비대칭이다. 원문 표기를 보존했다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical page 820의 명령 표기를 확인했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
---

## 개요

### 점검 내용

SNMP Community String 복잡성 적용 여부 점검

### 점검 목적

SNMP Community String이 유추하기 어려운 문자열로 구성해 비인가자가 SNMP 서비스 무단 접근을 방지하기 위함

### 보안 위협

SNMP Community String이 유추하기 쉬운 문자열로 구성되어 있는 경우 비인가자가 SNMP 서비스에 무단 접근이 가능한 위험이 존재함

### 참고

> **참고**
>
> SNMP v3 사용 우선 권고

## 점검 대상 및 판단 기준

### 대상

VMware ESXi, vCenter, XenServer, KVM, Nutanix

### 판단 기준

- **양호:** SNMP Community String이 복잡도를 만족하는 경우
- **취약:** SNMP Community String이 복잡도를 만족하지 않는 경우

### 조치 방법

SNMP Community String을 복잡도를 만족하는 값으로 설정

### 조치 시 영향

일반적 경우 영향 없음

## 점검 및 조치 사례

### LINUX

**VMware ESXi**

1. SSH를 통해 ESXi 호스트 서버 접속 후, 다음 명령어 실행

   ```bash command
   $ esxcli system snmp get
   ```

2. 다음 명령어를 사용해 Community String 값 설정 변경

   ```bash command
   $ esxcli system snmp set --communities <변경 값>
   ```

**VMware vCenter**

1. SNMP 사용 확인

   ```bash command
   if [[ $(vim-cmd proxysvc/service_list | grep 'TSM') ]]; then
       echo "SNMP service is running on vcenter“
   else
       echo "SNMP service is not running on vcenter“
   fi
   cat /etc/vmware/snmp.xml | grep community | awk -F '"' '{print $4}'
   ```

2. SNMP Community String 설정 확인

   ```bash command
   # /etc/snmpd.conf 파일에서 Community String 확인
   # /usr/lib/vmware/vm-support/bin/nvpsvc -printconfig | grep –E "SNMPCommunityString|SNMPAccessControl“
   ```

**KVM**

1. SNMP 파일에서 Community String 값 확인

   ```bash command
   sudo vi /etc/snmp/snmpd.conf
   ```

2. Community String 설정 후 snmp 서비스 재시작

   ```bash command
   sudo systemctl enable snmpd
   sudo systemctl start snmpd
   ```

**XenServer**

1. XenCenter 접속
2. 해당 서버 설정 > SNMP
3. SNMP 활성화 여부 확인
4. 활성화 또는 필요에 의해 사용 시 Community String 값 확인

**Nutanix**

**불필요한 경우 SNMP 서비스 비활성화**

1. Nutanix 관리 웹 콘솔 접속

   ```text literal
   https://<Nutanix 웹 콘솔>:9440
   ```

2. Settings > SNMP 선택
3. Enable for Nutanix objects 체크 해제

**SNMP Trap 설정이 필요한 경우 v3 사용**

1. Nutanix 관리 웹 콘솔 접속

   ```text literal
   https://<Nutanix 웹 콘솔>:9440
   ```

2. Settings > SNMP > Users > New User 클릭
3. SNMP User 생성
4. Traps > New Trap receiver 클릭
5. Receiver Name 및 Trap 수신 IP 설정

**v2c를 사용해야 하는 경우 Community String 복잡도 설정**

1. Nutanix 관리 웹 콘솔 접속

   ```text literal
   https://<Nutanix 웹 콘솔>:9440
   ```

2. Settings > SNMP > Traps > 편집 버튼
3. SNMP Community String이 복잡도를 만족하도록 설정
