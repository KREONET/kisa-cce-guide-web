---
schemaVersion: 1
contentModel: systemCriterion
contentModelVersion: 1

criterion:
  code: U-39
  slug: u-39
  title: 불필요한 NFS 서비스 비활성화
  severity:
    level: high
    sourceLabel: 상

classification:
  domainIdentifier: unix
  categoryIdentifier: unix-service-management

targetScope: nonExhaustive
targetIdentifiers:
  - solaris
  - linux
  - aix
  - hp-ux
sourceTargetText: "SOLARIS, LINUX, AIX, HP-UX 등"

provenance:
  sourceDocumentIdentifier: kisa-cce-criteria-2026
  sourcePageRanges:
    - physicalPageStart: 86
      physicalPageEnd: 88
      printedPageStart: "86"
      printedPageEnd: "88"

sourceAnnotations:
  - annotationIdentifier: u-39-source-001
    annotationType: sourceInconsistency
    targetType: astNode
    targetReference: "u-39:remediation.hp-ux.command:3"
    sourceLocation:
      physicalPage: 88
      printedPage: "88"
      pageRegionIdentifier: p88-u-39
    sourceText: "# /usr/sbin/nfs.server start"
    explanation: "NFS_SERVER=0으로 설정한 다음 NFS 서버를 start하는 명령을 제시한다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical pages 87-88의 HP-UX Step 3과 Step 4를 대조했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
---

## 개요

### 점검 내용

불필요한 NFS 서비스 사용 여부 점검

### 점검 목적

NFS(Network File System) 서비스는 한 서버의 파일을 많은 서비스 서버들이 공유하여 사용할 때 이용하는 서비스지만 이를 이용한 침해사고 위험성이 높으므로 사용하지 않는 경우 중지하기 위함

### 보안 위협

NFS 서비스는 서버의 디스크를 클라이언트와 공유하는 서비스로 적정한 보안 설정이 적용되어 있지 않다면 불필요한 파일 공유로 인한 유출 위험이 존재함

### 참고

> **참고**
>
> **NFS(Network File System):** 원격 컴퓨터의 파일 시스템을 로컬 시스템에 마운트하여 로컬 파일 시스템처럼 사용할 수 있는 프로그램

> **참고**
>
> NFS 서비스 사용은 원칙적으로 금지되어 있지만 불가피하게 사용 시 필요한 경우 U-40 항목을 참조하여 통제해야 함

## 점검 대상 및 판단 기준

### 대상

SOLARIS, LINUX, AIX, HP-UX 등

### 판단 기준

- **양호:** 불필요한 NFS 서비스 관련 데몬이 비활성화된 경우
- **취약:** 불필요한 NFS 서비스 관련 데몬이 활성화된 경우

### 조치 방법

NFS 서비스를 사용하지 않는 경우 서비스 중지 및 비활성화 설정 ※ 로컬 서버에 마운트 되어 있는 디렉터리 제거 및 공유 디렉터리 제거 후 서비스 중지 가능

### 조치 시 영향

일반적인 경우 영향 없음

## 점검 및 조치 사례

### SOLARIS

1. NFS 서비스 데몬 활성화 확인

   ```sh command
   # inetadm | egrep “nfs|statd|lockd”
   ```

2. 불필요한 서비스 데몬 중지

   ```sh command
   # inetadm -d <중지하고자 하는 서비스 데몬>
   ```

### LINUX

1. NFS 서비스 활성화 여부 확인

   ```sh command
   # systemctl list-units --type=service | grep nfs
   ```

2. 불필요한 NFS 서비스 중지

   ```sh command
   # systemctl stop <서비스명>
   ```

3. NFS 서비스 비활성화

   ```sh command
   # systemctl disable <서비스명>
   ```

### AIX

**process 점검**

1. NFS 프로세스 활성화 여부 확인

   ```sh command
   # ps -ef | grep nfsd
   ```

2. NFS 서비스 관련 데몬 중지

   ```sh command
   # kill –9 <PID>
   ```

3. NFS 시동 스크립트 위치 확인

   ```sh command
   # ls -al /etc/rc.d/rc*.d/* | grep nfs
   ```

4. NFS 시동 스크립트 이름 변경

   ```sh command
   # mv /etc/rc.d/rc2.d/S60nfs /etc/rc.d/rc2.d/_S60nfs
   ```

**service 점검**

1. NFS 서비스 활성화 여부 확인

   ```sh command
   # lssrc -a | grep nfs
   ```

2. NFS 서비스 관련 데몬 중지

   ```sh command
   # stopsrc -g nfs
   ```

3. `/etc/inittab` 파일 수정(주석 처리)

   ```text configuration
   # rcnfs:23456789:wait:/etc/rc.nfs > /dev/console
   # Start NFS Daemons
   ```

4. `/etc/inittab` 파일 설정 적용

   ```sh command
   #init q
   ```

### HP-UX

1. NFS 서비스 활성화 여부 확인 및 관련 데몬 PID 확인

   ```sh command
   # ps -ef | grep -E “nfsd|statd|lockd”
   ```

2. NFS 서비스 관련 데몬 중지

   ```sh command
   # kill -9 <PID>
   ```

3. `/etc/rc.config.d/nfsconf` 파일 수정

   ```text configuration
   NFS_SERVER=0
   ```

4. 설정 적용

   ```sh command
   # /usr/sbin/nfs.server start
   ```
