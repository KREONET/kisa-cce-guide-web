---
schemaVersion: 1
contentModel: systemCriterion
contentModelVersion: 1

criterion:
  code: U-45
  slug: u-45
  title: 메일 서비스 버전 점검
  severity:
    level: high
    sourceLabel: 상

classification:
  domainIdentifier: unix
  categoryIdentifier: unix-service-management

targetScope: nonExhaustive
targetIdentifiers:
  - solaris
  - linux
  - aix
  - hp-ux
sourceTargetText: "SOLARIS, LINUX, AIX, HP-UX 등"

provenance:
  sourceDocumentIdentifier: kisa-cce-criteria-2026
  sourcePageRanges:
    - physicalPageStart: 105
      physicalPageEnd: 110
      printedPageStart: "105"
      printedPageEnd: "110"

sourceAnnotations:
  - annotationIdentifier: u-45-source-001
    annotationType: sourceTypographicalError
    targetType: astNode
    targetReference: "u-45:remediation.solaris.command:1"
    sourceLocation:
      physicalPage: 105
      printedPage: "105"
      pageRegionIdentifier: p105-u-45
    sourceText: "/usr/sbin/sendmail -d grep Version"
    explanation: "원문 명령의 -d 뒤에 grep이 파이프 문자 없이 이어진다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical page 105의 명령을 확인했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
  - annotationIdentifier: u-45-source-002
    annotationType: sourceTypographicalError
    targetType: astNode
    targetReference: "u-45:remediation.solaris.command:7"
    sourceLocation:
      physicalPage: 106
      printedPage: "106"
      pageRegionIdentifier: p106-u-45
    sourceText: "usr/sbin/exim -bV"
    explanation: "원문의 실행 경로가 슬래시 없이 시작한다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical page 106의 명령을 확인했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
  - annotationIdentifier: u-45-source-003
    annotationType: sourceInconsistency
    targetType: astNode
    targetReference: "u-45:remediation.linux.step:10"
    sourceLocation:
      physicalPage: 107
      printedPage: "107"
      pageRegionIdentifier: p107-u-45
    sourceText: "Exim 서비스 활성화 여부 확인"
    explanation: "메일 서비스를 사용하는 경우의 첫 단계가 버전 확인이 아니라 서비스 활성화 여부 확인으로 기재되어 있다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical page 107의 Linux Exim 사용 절차를 확인했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
---

## 개요

### 점검 내용

취약한 버전의 메일 서비스 이용 여부 점검

### 점검 목적

메일 서비스 사용 목적 검토 및 취약점이 없는 버전의 사용 유무 점검으로 최적화된 메일 서비스의 운영하기 위함

### 보안 위협

취약점이 발견된 메일 버전의 경우 버퍼 오버플로우(Buffer Overflow) 공격에 의한 시스템 권한 획득 및 주요 정보 노출의 위험이 존재함

### 참고

> **참고**
>
> -

## 점검 대상 및 판단 기준

### 대상

SOLARIS, LINUX, AIX, HP-UX 등

### 판단 기준

- **양호:** 메일 서비스 버전이 최신 버전인 경우
- **취약:** 메일 서비스 버전이 최신 버전이 아닌 경우

### 조치 방법

메일 서비스를 사용하지 않는 경우 서비스 중지 및 비활성화 설정, 메일 서비스 사용 시 패치 관리 정책을 수립하여 주기적으로 패치 적용 설정

### 조치 시 영향

패치 적용 시 시스템 및 서비스의 영향 정도를 충분히 고려해야 함

## 점검 및 조치 사례

### SOLARIS

1. Sendmail 메일 서비스를 사용하는 경우, Sendmail 버전 확인

   ```shell command
   /usr/sbin/sendmail -d grep Version
   ```

2. 최신 버전 확인 및 보안 패치 진행. Sendmail 홈페이지(`http://www.sendmail.org/`)에 접속하여 다운로드 및 보안 패치 적용

3. Sendmail 메일 서비스를 사용하지 않는 경우, Sendmail 서비스 활성화 여부 확인

   ```shell command
   svcs -a | grep sendmail
   ```

4. Sendmail 서비스 비활성화

   ```shell command
   svcadm disable sendmail
   ```

1. Postfix 메일 서비스를 사용하는 경우, Postfix 버전 확인

   ```shell command
   /usr/lib/postfix/postconf | grep mail_version
   ```

2. 최신 버전 확인 및 보안 패치 진행. postfix 홈페이지(`https://www.postfix.org/packages.html`)에 접속하여 해당 OS 패키지 주소로 접속 후 다운로드 및 보안 패치 적용

3. Postfix 메일 서비스를 사용하지 않는 경우, Postfix 서비스 활성화 여부 확인

   ```shell command
   svcs -a | grep postfix
   ```

4. Postfix 서비스 비활성화

   ```shell command
   svcadm disable postfix
   ```

1. Exim 메일 서비스를 사용하는 경우, Exim 버전 확인

   ```shell command
   usr/sbin/exim -bV
   ```

2. 최신 버전 확인 및 보안 패치 진행. Exim 홈페이지(`https://www.exim.org/`)에 접속하여 다운로드 및 보안 패치 적용

3. Exim 메일 서비스를 사용하지 않는 경우, Exim 서비스 활성화 여부 확인

   ```shell command
   svcs -a | grep exim
   ```

4. Exim 서비스 비활성화

   ```shell command
   svcadm disable exim
   ```

### LINUX

1. Sendmail 메일 서비스를 사용하는 경우, Sendmail 버전 확인

   ```shell command
   sendmail -d0 -bt
   ```

2. 최신 버전 확인 및 보안 패치 진행. Sendmail 홈페이지(`http://www.sendmail.org/`)에 접속하여 다운로드 및 보안 패치 적용

3. Sendmail 메일 서비스를 사용하지 않는 경우, Sendmail 서비스 활성화 여부 확인

   ```shell command
   systemctl list-units --type=service | grep sendmail
   ```

4. Sendmail 서비스 중지

   ```shell command
   systemctl stop sendmail
   ```

5. Sendmail 서비스 비활성화

   ```shell command
   systemctl disable sendmail
   ```

1. Postfix 메일 서비스를 사용하는 경우, Postfix 버전 확인

   ```shell command
   postconf mail_version
   ```

2. 최신 버전 확인 및 보안 패치 진행. postfix 홈페이지(`https://www.postfix.org/packages.html`)에 접속하여 해당 OS 패키지 주소로 접속 후 다운로드 및 보안 패치 적용

3. Postfix 메일 서비스를 사용하지 않는 경우, Postfix 서비스 활성화 여부 및 PID 확인

   ```shell command
   ps -ef | grep postfix
   ```

4. Postfix 서비스 종료

   ```shell command
   kill -9 <PID>
   ```

1. Exim 메일 서비스를 사용하는 경우, Exim 서비스 활성화 여부 확인

   ```shell command
   systemctl list-units --type=service | grep exim
   ```

2. 최신 버전 확인 및 보안 패치 진행. Exim 홈페이지(`https://www.exim.org/`)에 접속하여 다운로드 및 보안 패치 적용

3. Exim 메일 서비스를 사용하지 않는 경우, Exim 서비스 활성화 여부 및 PID 확인

   ```shell command
   ps -ef | grep exim
   ```

4. Exim 서비스 종료

   ```shell command
   kill -9 <PID>
   ```

### AIX

1. Sendmail 메일 서비스를 사용하는 경우, Sendmail 버전 확인

   ```shell command
   sendmail -d0 -bt
   ```

2. 최신 버전 확인 및 보안 패치 진행. Sendmail 홈페이지(`http://www.sendmail.org/`)에 접속하여 다운로드 및 보안 패치 적용

3. Sendmail 메일 서비스를 사용하지 않는 경우, Sendmail 서비스 활성화 여부 확인

   ```shell command
   lssrc -a | grep sendmail
   ```

4. Sendmail 서비스 중지

   ```shell command
   stopsrc -s sendmail
   ```

5. `/etc/rc.tcpip` 파일 수정(주석 처리)

   ```text configuration
   #start /usr/lib/sendmail “$src_running” “-bd -q${qpi}”
   ```

1. Postfix 메일 서비스를 사용하는 경우, Postfix 버전 확인

   ```shell command
   postconf mail_version
   ```

2. 최신 버전 확인 및 보안 패치 진행. postfix 홈페이지(`https://www.postfix.org/packages.html`)에 접속하여 해당 OS 패키지 주소로 접속 후 다운로드 및 보안 패치 적용

3. Postfix 메일 서비스를 사용하지 않는 경우, Postfix 서비스 활성화 여부 및 PID 확인

   ```shell command
   ps -ef | grep postfix
   ```

4. Postfix 서비스 종료

   ```shell command
   kill -9 <PID>
   ```

1. Exim 메일 서비스를 사용하는 경우, Exim 버전 확인

   ```shell command
   exim -bV
   ```

2. 최신 버전 확인 및 보안 패치 진행. Exim 홈페이지(`https://www.exim.org/`)에 접속하여 다운로드 및 보안 패치 적용

3. Exim 메일 서비스를 사용하지 않는 경우, Exim 서비스 활성화 여부 및 PID 확인

   ```shell command
   ps -ef | grep exim
   ```

4. Exim 서비스 종료

   ```shell command
   kill -9 <PID>
   ```

### HP-UX

1. Sendmail 메일 서비스를 사용하는 경우, Sendmail 버전 확인

   ```shell command
   sendmail -d0 -bt
   ```

2. 최신 버전 확인 및 보안 패치 진행. Sendmail 홈페이지(`http://www.sendmail.org/`)에 접속하여 다운로드 및 보안 패치 적용

3. Sendmail 메일 서비스를 사용하지 않는 경우, Sendmail 서비스 활성화 여부 및 PID 확인

   ```shell command
   ps -ef | grep sendmail
   ```

4. Sendmail 서비스 종료

   ```shell command
   kill -9 <PID>
   ```

5. `/etc/rc.config.d/mailservs` 파일 수정

   ```text configuration
   SENDMAIL_SERVER=0
   ```

1. Postfix 메일 서비스를 사용하는 경우, Postfix 버전 확인

   ```shell command
   postconf mail_version
   ```

2. 최신 버전 확인 및 보안 패치 진행. postfix 홈페이지(`https://www.postfix.org/packages.html`)에 접속하여 해당 OS 패키지 주소로 접속 후 다운로드 및 보안 패치 적용

3. Postfix 메일 서비스를 사용하지 않는 경우, Postfix 서비스 활성화 여부 및 PID 확인

   ```shell command
   ps -ef | grep postfix
   ```

4. Postfix 서비스 종료

   ```shell command
   kill -9 <PID>
   ```

1. Exim 메일 서비스를 사용하는 경우, Exim 버전 확인

   ```shell command
   exim -bV
   ```

2. 최신 버전 확인 및 보안 패치 진행. Exim 홈페이지(`https://www.exim.org/`)에 접속하여 다운로드 및 보안 패치 적용

3. Exim 메일 서비스를 사용하지 않는 경우, Exim 서비스 활성화 여부 및 PID 확인

   ```shell command
   ps -ef | grep exim
   ```

4. Exim 서비스 종료

   ```shell command
   kill -9 <PID>
   ```
