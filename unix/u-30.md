---
schemaVersion: 1
contentModel: systemCriterion
contentModelVersion: 1

criterion:
  code: U-30
  slug: u-30
  title: UMASK 설정 관리
  severity:
    level: medium
    sourceLabel: 중

classification:
  domainIdentifier: unix
  categoryIdentifier: unix-file-directory-management

targetScope: nonExhaustive
targetIdentifiers:
  - solaris
  - linux
  - aix
  - hp-ux
sourceTargetText: "SOLARIS, LINUX, AIX, HP-UX 등"

provenance:
  sourceDocumentIdentifier: kisa-cce-criteria-2026
  sourcePageRanges:
    - physicalPageStart: 62
      physicalPageEnd: 64
      printedPageStart: "62"
      printedPageEnd: "64"

sourceAnnotations:
  - annotationIdentifier: u-30-source-001
    annotationType: sourceInconsistency
    targetType: astNode
    targetReference: "u-30:remediation.hp-ux.step:2"
    sourceLocation:
      physicalPage: 63
      printedPage: "63"
      pageRegionIdentifier: p63-u-30
    sourceText: "/etc/default/securitz"
    explanation: "경로의 securitz 표기가 원문에 그대로 나타나며, 정확한 경로인지 확인이 필요하다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical page 63의 HP-UX 두 번째 설정 파일 표기와 physical page 64의 이어지는 절차를 대조했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
  - annotationIdentifier: u-30-source-002
    annotationType: sourceInconsistency
    targetType: astNode
    targetReference: "u-30:remediation.supplementaryGuidance.step:2"
    sourceLocation:
      physicalPage: 64
      printedPage: "64"
      pageRegionIdentifier: p64-u-30
    sourceText: "ProFTP"
    explanation: "ProFTP의 첫 번째이자 유일한 절차가 원문에서 Step 2로 번호가 매겨져 있다."
    disposition: unresolved
    reviewStatus: pending
    verificationEvidence:
      - "PDF physical page 64의 vsFTP와 ProFTP 절차 번호를 대조했다."
    reviewedBy: null
    reviewedAt: null
    approvedBy: null
    approvedAt: null
---

## 개요

### 점검 내용

시스템 UMASK 값이 022 이상 설정 여부 점검

### 점검 목적

잘못 설정된 UMASK 값으로 인해 신규 파일에 대한 권한이 과도하게 부여되는 것을 방지하기 위함

### 보안 위협

잘못 설정된 UMASK로 인해 파일 및 디렉터리 생성 시 과도한 권한이 부여되어 무단 액세스 및 데이터 유출의 위험이 존재함

### 참고

> **참고**
>
> **UMASK:** 파일 및 디렉터리 생성 시 기본 권한을 지정해 주는 명령어

> **참고**
>
> 시스템 내에서 사용자가 새로 생성하는 파일의 접근 권한은 UMASK 값에 따라 정해지며, 계정의 환경설정 파일에 설정을 변경하면 사용자가 로그인한 후에도 변경된 UMASK 값을 적용받게 됨

> **참고**
>
> **Start Profile:** `/etc/profile`, `/etc/default/login`, `.cshrc`, `.kshrc`, `.bashrc`, `.login`, `.profile` 등

## 점검 대상 및 판단 기준

### 대상

SOLARIS, LINUX, AIX, HP-UX 등

### 판단 기준

- **양호:** UMASK 값이 022 이상으로 설정된 경우
- **취약:** UMASK 값이 022 미만으로 설정된 경우

### 조치 방법

설정 파일에 UMASK 값을 022로 설정

### 조치 시 영향

일반적인 경우 영향 없음

## 점검 및 조치 사례

### SOLARIS

1. `/etc/profile` 파일 내 UMASK 설정 확인 및 수정

   ```sh command
   # vi /etc/profile
   ```

   ```text configuration
   umask 022
   export umask
   ```

1. `/etc/default/login` 파일 내 UMASK 설정 확인 및 수정

   ```sh command
   # vi /etc/default/login
   ```

   ```text configuration
   UMASK=022
   ```

### LINUX

1. `/etc/profile` 파일 내 UMASK 설정 확인 및 수정

   ```sh command
   # vi /etc/profile
   ```

   ```text configuration
   umask 022
   export umask
   ```

1. `/etc/login.defs` 파일 내 UMASK 설정 확인 및 수정

   ```sh command
   # vi /etc/login.defs
   ```

   ```text configuration
   UMASK 022
   ```

### AIX

1. `/etc/profile` 파일 내 UMASK 설정 확인 및 수정

   ```sh command
   # vi /etc/profile
   ```

   ```text configuration
   umask 022
   export umask
   ```

1. `/etc/security/user` 파일 내 UMASK 설정 확인 및 수정

   ```sh command
   # vi /etc/security/user
   ```

   ```text configuration
   default : umask = 022 또는 <사용자 이름> : umask = 022
   ```

### HP-UX

1. `/etc/profile` 파일 내 UMASK 설정 확인 및 수정

   ```sh command
   # vi /etc/profile
   ```

   ```text configuration
   umask 022
   export umask
   ```

1. `/etc/default/securitz` 파일 내 UMASK 설정 수정

   ```text configuration
   UMASK = 022
   ```

### 추가 지침

FTP UMASK 설정

1. **vsFTP:** `{설치경로}/vsftpd.conf` 파일 내 UMASK 설정 확인 및 수정

   ```sh command
   # cat vsftpd.conf | grep local_umask
   ```

   ```text configuration
   local_umask=022
   ```

2. **ProFTP:** `{설치경로}/proftpd.conf` 파일 내 UMASK 설정 확인 및 수정

   ```sh command
   # cat proftpd.conf | grep umask
   ```

   ```text configuration
   Umask  022
   ```
